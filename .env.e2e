# Used by: npm run build:e2e
# Enables demo auth + disables MFA for Playwright against a production build.
# Never deploy this mode with real PHI.

VITE_ALLOW_DEMO_AUTH=true
VITE_DISABLE_MFA=true
VITE_PAYMENT_PROVIDER=mock
